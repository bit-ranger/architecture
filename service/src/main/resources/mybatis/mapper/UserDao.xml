<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rainyalley.architecture.service.user.dao.UserDao">

    <select id="select" resultType="com.rainyalley.architecture.service.user.model.entity.User">
        select * from user
        <where>
            <if test="id!=null">id=#{id}</if>
            <if test="name!=null">AND name=#{name}</if>
            <if test="password!=null">AND password=#{password}</if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into user(name,password,enabled) values(#{name},#{password},TRUE)
        <selectKey  resultType="int" keyProperty="id">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

    <delete id="delete">
        delete from user where id = #{id}
    </delete>

    <update id="update">
        update user
        <set>
            id=#{id}
            <if test="name!=null">name=#{name}</if>
            <if test="password!=null">password=#{password}</if>
        </set>
        where id = #{id}
    </update>
</mapper>
